
<%= form_with model: @blog_post do |form| %>
<% if form.object.errors.any? %>
  <div>
    <% form.object.errors.full_messages.each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>
<% end %>

  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
<br>
  <div>
    <%= form.label :cover_image %>
    <% if blog_post.cover_image.present? %>
      <div id="<%= dom_id(blog_post, :cover_image) %>">
        <%= image_tag blog_post.cover_image,  style: "width:300px; height:200px" %>
        <br>
        <br>
        <%= link_to "Remove Cover Image", blog_post_cover_image_path(blog_post), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
      </div>
    <% end %>
    <br>
    <%= form.file_field :cover_image %>
  </div>
  <br>
  <div>
    <%= form.label :content %>
    <%= form.rich_text_area  :content, style: "width:100%; height:900px " %>
    <%#= form.text_field  :content, :class => "tinymce", :id => "tinymce"%>
  </div>
<br>

  <div>
    <%= form.label :published_at %>
    <%= form.datetime_select :published_at, include_blank: true %>
  </div>
<br>

  <%= form.submit "submit", id: "btn" %>
  <br>
  <% end %>


